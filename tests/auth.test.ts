import test from "ava";

import AuthHandler from '../src/handlers/authHandler';

// const handler = new AuthHandler();

test('login > should 400 if email is missing from the body', t => {
  // @TODO: Implement login with empty data.
});